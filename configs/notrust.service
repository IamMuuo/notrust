[Unit]
Description=NOTRUST idle container daemon
After=docker.service

[Service]
Type=simple
ExecStart=%h/.local/bin/notrustd --config=%h/.config/notrust/config.yaml
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
